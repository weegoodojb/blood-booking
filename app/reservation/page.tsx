"use client";
import { useState } from "react";
import { Header } from "@/components/Header";
import { supabase, phoneOnly, Slot } from "@/lib/supabase";
import Slots from "@/components/Slots";
const reasons=[['personal','개인 사정'],['schedule','일정 변경'],['health','건강 상태'],['duplicate','중복 예약'],['event_change','행사 일정 변경'],['other','기타']];
export default function Reservation(){const [form,setForm]=useState({name:"",phone:"",pin:""}),[r,setR]=useState<any>(),[slots,setSlots]=useState<Slot[]>([]),[msg,setMsg]=useState(""),[pinMode,setPinMode]=useState(false),[newSlot,setNewSlot]=useState(""),[cancelOpen,setCancelOpen]=useState(false),[reason,setReason]=useState(''),[note,setNote]=useState('');const find=async(e:React.FormEvent)=>{e.preventDefault();setMsg("");const {data}=await supabase.rpc('find_reservation',{p_name:form.name,p_phone:phoneOnly(form.phone),p_pin:form.pin});if(!data?.ok){setMsg('입력하신 예약정보를 확인해주세요.');return}setR(data.reservation);setSlots(data.slots)};const change=async()=>{const {data}=await supabase.rpc('change_reservation_slot',{p_reservation_id:r.id,p_pin:form.pin,p_slot_id:newSlot});if(!data?.ok){setMsg(data?.message||'시간 변경에 실패했습니다.');return}setMsg('예약 시간이 변경되었습니다.');setR(data.reservation);setNewSlot("")};const cancel=async()=>{const {data}=await supabase.rpc('cancel_reservation',{p_reservation_id:r.id,p_pin:form.pin,p_reason:reason,p_note:note});if(data?.ok){setR(null);setMsg('예약이 취소되었습니다.')}else setMsg(data?.message||'취소 사유를 선택해주세요.')};const request=async(e:React.FormEvent)=>{e.preventDefault();const {data}=await supabase.rpc('create_pin_request',{p_name:form.name,p_phone:phoneOnly(form.phone)});setMsg(data?.ok?'PIN 변경 요청이 접수되었습니다. 담당자가 전화 확인 후 처리합니다.':'입력하신 정보를 확인해주세요.')};if(pinMode)return <main className="page"><Header back/><h1>PIN 변경 요청</h1><p className="hint">이름과 전화번호를 입력하면 담당자가 본인 확인 후 PIN을 변경합니다.</p><form onSubmit={request}><label className="label">이름</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><label className="label">전화번호</label><input required inputMode="numeric" value={form.phone} onChange={e=>setForm({...form,phone:phoneOnly(e.target.value)})}/>{msg&&<p style={{color:'#1e4fa3'}}>{msg}</p>}<button className="btn" style={{marginTop:18}}>PIN 변경 요청</button></form></main>;return <main className="page"><Header back/><h1>예약 조회 / 수정</h1>{!r?<form onSubmit={find}><p className="hint">예약 시 입력한 정보를 입력해주세요.</p><label className="label">이름</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><label className="label">전화번호</label><input required inputMode="numeric" value={form.phone} onChange={e=>setForm({...form,phone:phoneOnly(e.target.value)})}/><label className="label">예약 PIN (숫자 4자리)</label><input required maxLength={4} inputMode="numeric" value={form.pin} onChange={e=>setForm({...form,pin:phoneOnly(e.target.value).slice(0,4)})}/>{msg&&<p style={{color:'#bd293b'}}>{msg}</p>}<button className="btn" style={{marginTop:18}}>조회하기</button></form>:<section><div className="card"><b>{r.name}</b><p className="hint">{r.event_date} · {r.slot_time?.slice(0,5)}<br/>{r.location}</p></div><h2 style={{fontSize:'1.1rem',marginTop:25}}>예약 시간 변경</h2><Slots slots={slots} selected={newSlot} onSelect={setNewSlot}/>{newSlot&&<button className="btn" onClick={change}>선택한 시간으로 변경</button>}<button className="btn btn-outline" style={{marginTop:18}} onClick={()=>setCancelOpen(!cancelOpen)}>예약 취소</button>{cancelOpen&&<div className="card" style={{marginTop:12}}><label className="label">취소 사유</label><select value={reason} onChange={e=>setReason(e.target.value)}><option value="">선택해주세요</option>{reasons.map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</select>{reason==='other'&&<><label className="label">기타 사유</label><input value={note} onChange={e=>setNote(e.target.value)} placeholder="취소 사유를 입력해주세요"/></>}<button className="btn" onClick={cancel} style={{marginTop:16}}>취소 확정</button></div>}</section>}<button className="btn btn-outline" style={{marginTop:20}} onClick={()=>{setPinMode(true);setMsg('')}}>예약 PIN을 잊으셨나요?</button></main>}
